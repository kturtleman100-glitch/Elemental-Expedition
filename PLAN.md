# Elemental Expedition — 개발 계획서 및 진행 상황

> **새 세션을 시작했다면 이 파일을 먼저 끝까지 읽는다.**
> 아래 「3. 진행 상황」의 첫 미완료 항목이 곧 다음에 할 일이다.
> 작업을 마칠 때마다 체크박스와 「4. 마지막 작업 지점」을 갱신하고 커밋한다.

---

## 1. 프로젝트 개요

원소를 의인화한 3D RPG. 무대는 **아스티온 대륙**이고, 플레이어는 **119번 — 아직 발견되지 않은 원소**다.
주기율표 어디에도 칸이 없어 이름도 족(族)도 없기에 어느 세력에도 속하지 않으며,
그래서 모든 세력을 오갈 수 있는 유일한 존재다. **이름을 얻는 것이 최종 목표**다.

세계관은 사용자가 제공한 `참고자료/` (원소 의인화 설정집 사진 11장)에서 추출했다.
38명의 캐릭터마다 직업·성격·대사·소속 세력과 "인연이 깊은 원소" 관계망이 정의되어 있다.

### 절대 규칙

1. **모든 파일은 이 프로젝트 폴더 안에만 만든다.**
2. **`참고자료/`는 절대 커밋하지 않는다.** 책 스캔본이고 저장소가 **public**이다. `.gitignore`에 등재되어 있으니 해제하지 말 것.
3. 참고자료의 일러스트를 복제하지 않는다. 색·직업·실루엣만 참고해 원본 지오메트리로 만든다.
4. 커밋 메시지는 한국어로 쓴다.

---

## 2. 확정된 기술 결정

| 항목 | 결정 |
|---|---|
| 렌더링 | Three.js `0.169.0`, CDN + importmap — **npm 설치·빌드 없음** |
| 아트 | **셀셰이딩(MeshToonMaterial 3단 계조) + 검은 외곽선 + 7등신 비율** |
| 시점 | 1인칭 ↔ 3인칭 전환 (`V`) |
| 게임 루프 | **적응형 고정 틱** — 프레임당 최소 1회, 성능 허용 범위에서 최대한 자주 |
| 대상 기기 | 데스크톱 · 노트북 · 모바일 전부 (터치 조작 포함) |
| 저장 | `localStorage` 자동 저장 + 슬롯 3개, 타이틀에서 **이어하기** |
| 멀티플레이 | **PeerJS(WebRTC P2P)** — 방 코드 입력, 배포할 백엔드 없음 |
| 배포 | **Vercel** (GitHub 연동, `git push` 시 자동 배포) |
| 저장소 | `kturtleman100-glitch/Elemental-Expedition` (**public**) |
| 엔딩 | **4종 분기** (평등 / 질서 / 안정의 섬(진) / 붕괴) |
| 캐릭터 에셋 | 절차적 생성이 기본. `assets/models/<id>.glb`가 있으면 그것으로 대체 |

### 조작

| 동작 | PC | 모바일 |
|---|---|---|
| 이동 | WASD | 좌측 가상 조이스틱 |
| 시선 | 마우스 (포인터 락) | 우측 드래그 |
| 공격 | 좌클릭 | 공격 버튼 |
| **상호작용** | **우클릭** (트랙패드 대체키 `F`) | 접근 시 뜨는 버튼 |
| 점프 | Space | 점프 버튼 |
| **인벤토리** | **`E`** | 상단 가방 아이콘 |
| **화합물 사용** | **`R`** | (미구현) |
| 자세 전환 (하이브리드) | `T` | (미구현) |
| 파티 / 도감 / 퀘스트 | `P` / `K` / `J` | 상단 메뉴 |
| 시점 전환 | `V` | 전환 버튼 |
| 락온 타겟 전환 | `Tab` / 휠 | 타겟 아이콘 |
| 메뉴·저장 | `Esc` | 메뉴 버튼 |

---

## 3. 진행 상황

### 1단계 — 움직이는 3D 월드 · 완료 (커밋 `e63cbc8`)

- [x] `index.html` — importmap으로 three.js CDN 로드, 로딩·타이틀 화면
- [x] `src/core/Loop.js` — 적응형 고정 틱 (기기별 120/90/60Hz, 실측 자동 조정, MAX_TICKS=5)
- [x] `src/core/Device.js` — 기기 판별, 3단계 성능 프리셋
- [x] `src/core/Input.js` — 키보드·마우스·터치 통합 입력
- [x] `src/core/Game.js` — 시스템 등록, 업데이트/렌더 분리
- [x] `src/world/Collision.js` — AABB 충돌
- [x] `src/player/Player.js` — 이동·점프·중력
- [x] `src/player/CameraRig.js` — 1인칭/3인칭 보간 전환
- [x] `src/ui/TouchControls.js` — 가상 조이스틱, 터치 버튼

### 2단계 — Git 저장소 + 배포 준비 · 완료

- [x] `.gitignore` — `참고자료/` 제외 확인
- [x] `git init` → remote 연결 → push
- [x] `vercel.json` 작성
- [x] **Vercel 연결 완료** — https://elemental-expedition.vercel.app (`git push` 시 자동 배포)

### 아트 디렉션 전환 · 완료 (커밋 `806379d`)

이전의 단색 저폴리가 "유치해 보인다"는 지적을 받아 전면 교체했다.

- [x] `src/fx/Style.js` — 셀셰이딩 머티리얼·외곽선·3점 조명·그라데이션 스카이돔
- [x] `src/characters/CharacterBuilder.js` — 7등신 절차적 캐릭터 + 걷기 애니메이션
- [x] `src/data/elements.js` — **원소 38명 + 플레이어** 전체 데이터
- [x] `src/world/World.js` — 토룡마을 재구성 (가옥 5채·우물·석회암 노두·언덕·길)
- [x] A/D 좌우 반전 버그 수정 (카메라 상대 이동의 우측 벡터 부호)
- [x] `[hidden]` CSS 우선순위 버그 수정 (로딩 화면이 안 사라지던 문제)

### 3단계 — 캐릭터 배치와 대화 · 완료 (커밋 `b774803`)

- [x] `src/data/factions.js` — 7개 세력 + 평판 수치(−100~100)
- [x] `src/data/dialogue.js` — 6인 대사 트리, 선택지 분기
- [x] `src/characters/NPC.js` — 모델 배치, 접근 감지, **우클릭 대화**, 거리 컬링
- [x] `src/ui/Dialogue.js` — 주기율표 칸 모양 화자 표시, 타자기 효과
- [x] `src/ui/Codex.js` — 원소 도감 (`K`), 만난 원소 자동 등록
- [x] 토룡마을에 **칼슘·인·탄소·규소·마그네슘·철** 6명 배치
- [x] `src/characters/CharacterLoader.js` — `.vrm`이 있으면 로드, 없으면 절차적 생성

**검증**: NPC에게 우클릭하면 자료의 성격이 살아있는 대사가 나오고 도감에 등록된다.

### 4단계 — 전투·상성·락온 카메라 · 완료 (커밋 `ff61d9c`)

- [x] `src/combat/Electron.js` — 전자(e⁻) 자원. 방출형/흡수형/무반응
- [x] `src/combat/CombatStyle.js` — 마법형/무기형/하이브리드. **`T`로 자세 전환**
- [x] `src/combat/DamageCalc.js` — 전기음성도 상성 + 유형별 배율
- [x] `src/combat/TargetLock.js` — 화면 중앙에 가까운 적 우선
- [x] `src/player/CameraRig.js` 확장 — 락온 모드, 설정에 끄기 옵션
- [x] `src/combat/Enemy.js` — 적 AI + 전용 밸런스 계수, 적끼리 밀어내기
- [x] `src/combat/Projectile.js` — 마법 투사체 풀 24개 (계획에 없던 추가분)
- [x] `src/fx/Particles.js` — 족별 파티클 900개를 Points 하나로
- [x] `src/ui/HUD.js` — HP·전자량·경험치, 배율 표시("×2.07 격렬 반응!")

**주의**: 적 AI와 데미지 판정에 "이 개체를 누가 판정하는가"를 **처음부터 인자로 둘 것.**
9단계 멀티플레이에서 호스트만 판정하도록 바꿔야 하는데, 나중에 끼워 넣으면 전투 코드를 통째로 뜯게 된다.

### 5단계 — 성장·원소 획득·파티·인연 · 완료 (커밋 `7f8d99d`)

- [x] `src/player/Progress.js` — 레벨업 곡선, 원소 보유·장착
- [x] `src/data/bonds.js` — 이름 있는 인연 8종 + 화합물 10종 (전부 실제 물질)
- [x] `src/data/quests.js` — 1·2장 퀘스트 5개. **보상이 원소 그 자체**
- [x] `src/ui/PartyUI.js` — 슬롯 4칸, 인연 실시간 표시, 능력치 미리보기
- [x] `src/ui/Inventory.js` — 화합물 조합 (`E`)
- [x] `src/ui/Quest.js` — 우측 상시 표시 (`J`로 접기)
- [x] `src/ui/Minimap.js` — 정적 지형은 오프스크린에 한 번만
- [x] **마을 밖 야외** — 북 석회암 고원 · 동 강·다리·숲 · 서 폐허 · 남 평원·연못
- [x] `src/characters/PartyMember.js` — 동료 AI. 편성한 원소가 따라다니며 함께 싸운다

### 화합물 사용 · 완료 (계획에 없던 단계)

조합법을 익히는 것까지는 5단계에 있었는데 **쓸 방법이 없었다.**
만들기만 하고 못 쓰면 조합표는 그냥 읽을거리가 된다.

- [x] `src/combat/Compound.js` — R 키로 사용. 전자 소모 + 6초 쿨다운
- [x] 효과 10종이 전부 실제로 동작한다 (넷은 값만 적혀 있고 아무도 안 읽었다)
- [x] 얼음(H₂O 고체) 추가 — 물과 같은 화합물의 다른 상태

### 6단계 — 저장과 이어하기 · 완료 (커밋 `50f39ec`)

- [x] `src/core/SaveData.js` — localStorage 스키마 + **버전 마이그레이션**(v3)
- [x] `src/ui/SaveMenu.js` — 슬롯 3개, 설정 포함 (`Esc`)
- [x] 타이틀의 **이어하기** — 가장 최근 슬롯을 열고, 없으면 비활성화
- [x] 자동 저장 — 60초 주기 + 퀘스트 완료 시
- [x] 설정 — 감도·카메라 자동추적·그림자·파티클

**검증**: 브라우저를 완전히 닫고 다시 열어도 위치·레벨·인벤토리·퀘스트가 복원된다.
시크릿 모드에서도 저장만 안 될 뿐 게임은 정상 실행된다 (`try/catch` 필수).

### 7단계 — 지역·엔딩 분기 · 완료 (커밋 `50f39ec`)

- [x] `src/world/Zone.js` — 5개 지역의 안개·조명을 서서히 전환
- [x] `src/ui/Quest.js` — 퀘스트 추적 (5단계에서 먼저 완료)
- [x] `src/data/endings.js` — 4개 엔딩 판정 + 회차 이월
- [x] `src/ui/Cinematic.js` — 자막·엔딩 연출
- [x] 세력 평판 수치, 엔딩 플래그 누적
- [ ] 나머지 5개 지역의 **실제 지형** (탄소 학교·아르곤 시티·철의 요새·대성당·온천)

### 8단계 — 보스 6인 · 완료 (커밋 `50f39ec`)

- [x] `src/data/bosses.js` — 조무래기 2 · 중급 3 · 파멸급 1
- [x] `src/combat/Boss.js` — Enemy 상속 + 페이즈·기믹, 염소 설득 분기
- [x] 4개 엔딩 연출
- [x] **기믹 14종이 전부 실제로 동작한다** — 예전에는 파티클만 뿌리고 아무 일도
      하지 않았다. 보스가 화려하게 연출만 하고 실제로는 평타만 때린 셈이었다
  · 장판(독·방사선)은 0.5초마다 판정한다. 매 틱이면 스치기만 해도 즉사한다
  · 브로민의 가짜 뉴스는 **표시만** 거짓이고 실제 피해는 진짜 상성대로 들어간다
  · 폴로늄의 포탑은 일반 적으로 세운다 — 락온·피해·사망이 그대로 돈다
  · 수은의 매혹은 동료 하나를 잠시 빼앗는다. 적으로 돌리면 대응할 방법이 없다
- [x] **염소 설득** — `tryPersuade`를 아무도 부르지 않아 진엔딩 조건에
      도달할 수 없었다. 설득 창이 열리면 안내를 띄우고 상호작용 키로 잇는다

### 무한 대륙 · 완료 (계획에 없던 단계)

"지역을 마인크래프트처럼 거의 무한하게"라는 요청으로 들어왔다.
7단계에서 좌표에 원을 다섯 개 그려 만든 지역 시스템을 통째로 갈아엎었다 —
무한한 세계에는 원을 무한히 그릴 수 없다.

- [x] `src/world/Noise.js` — 결정론적 노이즈. 저장할 것은 시드 하나뿐이다
- [x] `src/world/Biome.js` — 열 가지 땅. 전부 실제 원소의 성질에서 나왔다
- [x] `src/world/Terrain.js` — 높이와 바이옴의 **유일한** 근거
- [x] `src/world/Chunk.js` — 64m 조각. 지형·물·소품·충돌을 자족적으로 들고 있다
- [x] `src/world/ChunkManager.js` — 스트리밍 (프레임 예산 · 히스테리시스)
- [x] `src/world/Collision.js` 격자 분할 — 수천 개 상자를 120Hz로 훑을 수 없다
- [x] 청크마다 야생 몬스터. 그 땅에 살 법한 원소가 나온다
- [x] 물 — 수면 −1.2m, 1.35m를 넘으면 헤엄친다
- [ ] 나머지 5개 지역의 **손으로 지은** 지형 (탄소 학교·아르곤 시티·철의 요새·대성당·온천)

### 9단계 — 멀티플레이 · **← 남은 단계**

- [ ] `src/net/Transport.js` (PeerJS를 교체 가능하게 인터페이스로 감쌈)
- [ ] `src/net/Host.js`, `src/net/Guest.js`, `src/net/RoomCode.js`
- [ ] `src/ui/MultiplayerUI.js`

전투 코드에 `authority` 인자를 처음부터 넣어뒀으므로(4단계), 호스트만 판정하도록
바꾸는 작업이 비교적 수월하다. 지형도 시드 하나로 복원되므로 세계를 주고받을
필요가 없다 — 좌표만 맞으면 둘이 같은 땅을 본다.

---

## 4. 마지막 작업 지점

- **날짜**: 2026-09-02
- **마지막 커밋**: `21675ff`
- **완료**: 1~8단계 + **무한 대륙** + **화합물 사용**
- **남은 것**: 9단계 멀티플레이

### 배포

**https://elemental-expedition.vercel.app** — `git push` 시 자동 배포.

> **Vercel의 공유 설정은 절대 건드리지 않는다.** 사용자가 명시적으로 지시했다.
> 배포는 `git push`로만 하고, 대시보드 설정은 사용자가 직접 한다.

### 커밋 전 검사

```
bash tools/check.sh
```

네 가지를 함께 본다. 셋은 이 프로젝트가 겪은 실제 사고에서 나왔다.

| 검사 | 잡는 것 | 왜 필요했나 |
|---|---|---|
| 문법 (`node --check`) | 파일 하나짜리 오타 | — |
| **import 무결성** | 없는 이름을 import | `node --check`는 파일을 하나씩만 봐서 못 잡고 브라우저에서야 터진다 |
| **화학 검증** | 틀린 원자량·전기음성도·족, 만들 수 없는 화합물 | 틀린 값은 재미가 아니라 오개념을 가르친다 |
| **지형 검증** | 청크 이음매·바이옴 분포·경사·결정성 | 브라우저로는 먼 땅에 가 볼 수도 없다 |

현재 상태: **화학 오류 0 · 학습 공백 1 · 각색 0 / 지형 검사 통과**

### 브라우저를 볼 수 없을 때 쓰는 탐침

이 프로젝트의 버그 절반은 "화면이 이상하다"로 들어왔는데, 화면을 직접 볼 수가
없다. 그래서 헤드리스 크롬을 붙여 **숫자로 재는** 도구를 만들었다.

```
node tools/probe.mjs [주소] [대기ms] ["평가할 JS"]   # 콘솔 오류 + 화면 상태
node tools/probe-walk.mjs [주소] [초]                # 베타로 시작해 걸어 본다
node tools/probe-ground.mjs [주소]                   # 지면과 소품 높이를 잰다
```

`window.__debug = { scene, world, player, camera, renderer }` 가 열려 있어
탐침이 씬을 직접 읽는다. **게임 로직은 이걸 절대 읽지 않는다** — 읽는 순간
디버그 손잡이가 사양이 된다.

### 무한 대륙

시드 하나(`Terrain.seed`)에서 대륙 전체가 계산된다. 저장할 것도 그것뿐이다.

| 값 | 뜻 |
|---|---|
| 청크 64m, 시야 반경 3 (192m) | 안개가 그 앞에서 닫혀 경계가 안 보인다 |
| 평지 반경 225m | **손으로 지은 세계가 220m까지 뻗는다.** 그 안은 굴곡이 없어야 한다 |
| 소품 제외 240m | 길 끝에 나무가 자라지 않게 평지보다 조금 넓게 |
| 수면 −1.2m | 하나로 고정. 여기저기 다르면 어디가 물인지 눈으로 못 가늠한다 |
| 바다 1800~2100m | 사실상 무한이지만 끝은 있다 |

**바이옴 10종은 전부 실제 화학에서 나왔다.**

| 땅 | 근거 |
|---|---|
| 소금 평원 | NaCl 결정이 입방정계라 정육면체로 솟는다 |
| 붉은 대지 | 철이 산소에 전자를 빼앗긴 Fe₂O₃ |
| 수정 지대 | SiO₂가 육각기둥으로 자란다 |
| 석회암 고원 | CaCO₃. 빗물에 녹아 동굴이 뚫린다 |
| 유황 온천 | 황이 115도에 녹아 땅속을 흐른다 |
| 방사성 황무지 | 붕괴 에너지가 빛으로 나와 바위가 발광한다 |

처음 밟는 땅에서만 그 화학이 자막으로 한 번 뜬다.

### 게임이 가르치는 것

플레이어가 규칙을 외우는 대신 **화학으로 추측할 수 있게** 만드는 것이 목표다.

- **족의 공통 성질** — 도감(K)의 접이식 해설이 trait / why(전자 배치) /
  inGame(게임 규칙으로 어떻게 나타나는가) / watch(직접 비교해 볼 수치)를 보여준다.
  예: 할로겐은 바깥 전자가 일곱이라 하나만 모자라다 → 그래서 전자를 뺏는다 →
  그래서 게임에서 탈취형이다 → 그래서 염소가 아르곤을 노린다
- **화합물** — 인벤토리(E)의 11종에 결합 종류와 화학 설명. CaF₂의 아래 첨자 2가
  왜 2인지, CO₂가 왜 무극성인지까지 적었다
- **효과가 성질에서 나온다** — 소금은 단단하니 꿰뚫고, 물은 알칼리 금속에 연쇄
  폭발하고, 납은 밀도가 높으니 피해를 막는다. 조합표를 외우지 않아도 짐작할 수 있다
- **예외를 드러내기** — 족 규칙을 어기는 다섯(브로민·마그네슘·베릴륨·수은·비소)은
  왜 어기는지 도감에 적는다. 설명 없이 두면 "마그네슘은 비금속인가 보다"라고
  잘못 배운다. **검사기가 이유 없는 예외를 오류로 잡는다**

### 편성 규칙

원소를 얻어도 **자동으로 장착되지 않는다.** 맨 처음 하나만 자동으로 쥐어 주고,
그 뒤로는 `P`에서 직접 고른다. 예전에는 빈 슬롯마다 자동으로 채워서 먼저 만난
넷이 그대로 편성이 됐고, 무엇을 쓸지 고르는 재미가 없었다.

슬롯은 자리를 지킨다 — 빈 칸을 걷어내면 3번에 둔 것이 1번으로 밀려나
원소를 하나 뺄 때마다 편성이 흐트러진다.

### 베타 모드

타이틀의 **베타 · 전체 해금** — 원소 43종·레벨 30·분기 플래그를 다 열어
인벤토리·파티·도감처럼 "다 모아야 보이는" 화면을 바로 검증할 수 있다.
베타 중에는 자동 저장을 끈다(한 번 눌러본 것만으로 정식 저장이 덮어써지면 안 된다).

### 다음에 할 일 (우선순위 순)

1. **실제 플레이 검증** — 1장부터 끝까지 한 번 통과해 보며 막히는 곳 찾기
2. **보스 기믹** — 페이즈 전환은 되지만 포탑·독 장판이 비어 있다.
   매혹은 화합물 쪽에 이미 있으니 그대로 쓰면 된다
3. **VRM 용량** — 9종 125MB. PNG→WebP로 절반을 줄일 수 있다.
   43명을 다 만들면 저장소가 감당하지 못한다
4. **인연이 가리키는 미구현 원소 18종** — 여럿이 동시에 가리키는 것부터
   (주석 5곳, 텅스텐 3곳, 알루미늄 3곳). 그만큼 인연 보너스가 죽어 있다
5. **나머지 5개 지역**의 손으로 지은 지형
6. **9단계 멀티플레이**

### 캐릭터 에셋 (VRM 9종, 사용자 제작)

| 원소 | 파일 | 역할 |
|---|---|---|
| 119번 | `uue.vrm` | 플레이어 |
| 칼슘 Ca | `ca.vrm` | 토룡마을 촌장 |
| 탄소 C | `c.vrm` | 학교 교장 |
| 규소 Si | `si.vrm` | 예언가 |
| 인 P | `p.vrm` | 대안통운 택배 기사 |
| 마그네슘 Mg | `mg.vrm` | 숲의 주인 |
| 철 Fe | `fe.vrm` | 전이 금속 군단 대장 |
| 수은 Hg | `hg.vrm` | 3장 중급 보스 |
| 금 Au | `au.vrm` | 귀금속 귀족 |

**새 `.vrm`을 넣는 절차**
1. `assets/models/<원소id>.vrm` 에 복사
2. `src/characters/CharacterLoader.js`의 `AVAILABLE`에 id 추가
3. `node tools/strip-vrm-thumbnail.mjs assets/models/*.vrm`

---

## 5. 수행 이력

계획서의 단계 번호와 실제 작업 순서가 완전히 일치하지는 않는다.
사용자 요청("마을이 좁다", "몬스터가 사기다", "맵을 넓혀라")이 들어올 때마다
해당 단계를 앞당기거나 계획에 없던 작업을 끼워 넣었기 때문이다.
아래가 **실제로 일어난 순서**다.

| 커밋 | 내용 | 계획 대비 |
|---|---|---|
| `e63cbc8` | **1단계** 움직이는 3D 월드 (적응형 고정 틱, PC/모바일 입력, 1·3인칭) | 계획대로 |
| `806379d` | **아트 전환** 셀셰이딩 + 7등신, 원소 38명 데이터 | 계획에 없음 — "유치해 보인다"는 지적 |
| `d9fb13e` | **재개용 계획서** PLAN.md + CLAUDE.md | 계획에 없음 — 세션 중단 대비 |
| `a66b079` | **마을 텍스처·인스턴싱**, 지역 컨셉 페이지 | 계획에 없음 — "장난감 같다" |
| `81c21b2` | **마을 확장** 지름 50m→130m, 5구역. 지오메트리 병합 (드로우콜 698→138) | 7단계 일부를 앞당김 |
| `b774803` | **3단계** NPC 배치·대화·원소 도감 | 계획대로 |
| `ff61d9c` | **4단계** 전자 전투·전기음성도 상성·마법 투사체·락온 | 계획대로 + 투사체는 추가분 |
| `7f8d99d` | **5단계** 인연·화합물·파티·인벤토리·미니맵 + **마을 밖 야외** | 야외는 7단계를 앞당긴 것 |
| `50f39ec` | **6·7·8단계** 저장·지역 분위기·엔딩·보스 6인·동료 AI | 사용자 요청으로 연속 진행 |
| `2a8cf17` | **화학 검증 도구** + 베타 전체 해금 + 저장 슬롯 삭제 | 계획에 없음 — "화학이 잘 들어갔는지 검증해" |
| `38744db` | **도감·인벤토리에 화학 설명** (족 해설 11종, 화합물 결합·사실) | 계획에 없음 — "놀다 보면 화학이 남게" |
| `08abb7b` | 족 규칙의 **예외를 명시**하고 검사기가 강제 | 계획에 없음 |
| `c5919a4` | **무한 지형** — 노이즈·바이옴·청크·스트리밍 | 계획에 없음 — "마인크래프트처럼 무한하게" |
| `fbe1640`~`bec09e2` | 무한 지형이 깨뜨린 것들 수습 (z-파이팅, 뜨는 몬스터, 평지 범위) | 위 작업의 뒤처리 |
| `bfe86f1` | 청크 경계 자연화 · 야생 몬스터 · **화합물 사용** · 하늘 추종 | 계획에 없음 |
| `21675ff` | 안 되던 화합물 4종 구현 · 얼음 · 물에 빠지기 · **편성 선택** | 계획에 없음 |

### 계획에 없던 작업이 들어온 이유

| 사용자 지적 | 대응 |
|---|---|
| "그래픽이 유치하다" | 단색 저폴리 → 셀셰이딩 + 7등신 + 절차적 텍스처 9종 |
| "장난감 같다" | 밀도 3배, 담장·좌판·빨래줄 등 생활의 흔적 추가 |
| "마을이 좁다" | 지름 50m → 130m, 5개 구역으로 분리 |
| "맵을 마을 밖으로도" | 북 고원·동 강과 숲·서 폐허·남 평원. 경계 R=178 |
| "집이 길을 막는다" | 검사 스크립트로 전수 확인 후 재배치 |
| "몬스터가 사기다" | 적 전용 계수 도입 + 검산 스크립트로 수치 확인 |
| "랙이 심하다" | 나침반 재그리기 억제, 파티클 업로드 생략, 그림자 추종 |
| VRM 9종 제작 | `CharacterLoader`, 썸네일 제거 도구, 병렬 로드 |
| "화학이 잘 들어갔는지 검증해" | 기준표 대조 검사기. **실제 오류 6건**을 잡아냈다 |
| "놀다 보면 화학을 알 수 있게" | 족 해설 11종 + 화합물 결합·사실 + 예외 명시 |
| "맵을 마인크래프트처럼 무한하게" | 시드 기반 절차적 지형 + 청크 스트리밍 + 바이옴 10종 |
| "청크가 자연스럽게 연결되게" | 색·소품을 청크 중심이 아니라 **정점 자리의** 바이옴에서 |
| "만들어도 사용을 어떻게해?" | `R` 키 화합물 사용. 넷은 값만 있고 아무도 안 읽고 있었다 |
| "선착순 편성이 재미없다" | 자동 장착을 **맨 처음 하나만**으로. 슬롯이 자리를 지키게 |
| "물에 빠지게" | 수면 −1.2m, 깊이에 따라 걷기/헤엄 |

### 고친 버그 (원인이 특이했던 것만)

| 증상 | 원인 |
|---|---|
| 로딩 화면이 안 사라짐 | `#loading-screen`의 `display:flex`가 ID 선택자라 `[hidden]`을 이겼다 |
| A/D가 반대로 | 우리 좌표계(정면 +Z)에서 화면상 오른쪽은 월드 **−X**인데 +X로 계산 |
| 점프가 안 됨 | `clearFrame()`이 `update`보다 먼저 실행되어 플래그가 미리 지워졌다 |
| 움직이는데 UI가 없음 | `const hud`가 DOM 요소를 가려 `#hud`가 계속 숨겨져 있었다 |
| 매 프레임 멈춤 | `speedMult`를 선언 전에 사용 (TDZ ReferenceError) |
| 팔이 몸을 관통 | `animateCharacter`의 `rotation.z` 부호가 `buildArms`와 반대 |
| 왼팔을 휘두름 | +X 팔에 `rightArm` 이름을 붙였다 (캐릭터의 왼쪽이 +X) |
| 카메라가 벽을 뚫음 | 카메라에 충돌 판정 자체가 없었다 |
| 석회암·나무 잎이 사라짐 | `Icosahedron`은 인덱스가 없고 `Box`는 있어서 병합 실패 |
| 마법이 전자를 안 씀 | 흡수형(할로겐)에 소모를 면제해둬서 무한 마법이 됐다 |
| **화면 오류 상자가 한 번도 안 뜸** | 그 스크립트 자체가 문자열 안에 진짜 개행을 담아 파싱 단계에서 죽었다. 즉 오류 핸들러가 등록된 적이 없다 |
| 로딩이 0%에서 멈춤 | `vercel.json`이 `/src/`를 1시간 캐시. 빌드가 없어 파일명에 해시가 안 붙으니 **옛 모듈과 새 모듈이 섞여** 들어왔다 |
| 땅이 얼룩덜룩 깜빡임 | 마을 풀밭 판과 청크 지형이 마을 안에서 **정확히 같은 높이**에 겹쳤다(z-파이팅) |
| 몬스터가 공중에 뜸 | `Enemy.js`가 매 프레임 메시 y를 **0으로 못 박고** 있었다 |
| 마을 나무 밑동이 잘림 | 손으로 지은 세계는 220m까지 뻗는데 평지 고정은 62m뿐이었다 |
| 뒤에 거대한 검은 반구 | 하늘돔이 원점 반경 400m 고정이라 **걸어서 하늘 밖으로 나갔다** |
| 투사체가 땅속에서 발사 | 절대 y=1.15 고정. 지형에 높이가 생기며 언덕에서 파묻혔다 |
| 화합물 4종이 무반응 | `debuff`·`charmed`·`reveal`·`barrier`를 아무도 읽지 않았다 |
| 편성이 뒤죽박죽 | `equip`이 빈 칸을 걷어내서 3번에 둔 것이 1번으로 밀려났다 |
| 포인터 락 오류 상자 | Esc 직후 재획득 거부는 정상 동작인데 그 거부를 아무도 안 받았다 |

### 만든 도구

**검사** (`bash tools/check.sh` 가 아래 넷을 한 번에 돌린다)

- `tools/check-imports.mjs` — 없는 이름을 import하는지. `node --check`가 못 잡는 영역
- `tools/check-chemistry.mjs` + `tools/chem-reference.mjs` — IUPAC 원자량·폴링
  전기음성도·족 분류·화합물을 실제 값과 대조. **실제 오류 6건**을 잡아냈다
- `tools/check-terrain.mjs` — 결정성·청크 이음매·바이옴 분포·경사·대륙의 끝

**탐침** (브라우저를 볼 수 없으니 헤드리스로 잰다)

- `tools/probe.mjs` — 콘솔 오류 + 화면 상태. 넷째 인자로 임의의 JS를 평가한다
- `tools/probe-walk.mjs` — 베타로 시작해 걸어 보고 청크·드로우콜·FPS를 읽는다
- `tools/probe-ground.mjs` — 지면과 소품 높이를 재서 어긋남을 찾는다

**에셋**

- `tools/strip-vrm-thumbnail.mjs` — VRM에서 안 쓰는 썸네일 제거 (파일당 1~1.5MB)
- `concept/zone-concepts.html` — 10개 지역 컨셉
- `concept/3d-character-guide.html` — 캐릭터 제작 안내서 + 진행 체크리스트
- `concept/village-art-proposal.html` — 마을 아트 현재/개선안 비교

### 검사 도구가 실제로 잡아낸 것

도구를 만드는 데 든 시간이 아깝지 않았던 이유다.

| 도구 | 잡아낸 것 |
|---|---|
| 화학 검증 | 폴로늄 원자량 210→**209**, 테크네튬 99→**98**·끓는점 4877→**4265** |
| 화학 검증 | 납·비스무트가 준금속으로 분류돼 있었다 (실제로는 전이후 금속) |
| 화학 검증 | **화합물이 요구하는 플루오린·수소·소듐이 원소 목록에 아예 없었다.** NaCl·H₂O·CaF₂가 영원히 제작 불가였다 |
| 화학 검증 | 알칼리 금속이 소듐 하나뿐이라 "1족은 아래로 갈수록 전자를 쉽게 내준다"를 비교할 대상이 없었다 → 포타슘 추가 |
| 지형 검증 | 마을 평지 범위가 실제 콘텐츠보다 좁다는 것 (검사 범위를 55m→220m로 넓히고 나서야 드러났다) |
| 탐침 | 하늘돔이 원점 고정이라는 것, 드로우콜 211→174 개선 확인 |

---

## 6. 게임 시스템 설계 (구현 시 참조)

### 전자(e⁻) — MP를 대체하는 핵심 자원

- **금속계**(전이 금속·알칼리·귀금속): 전자를 **방출** → 아군 버프, 무기 강화, 광역 공격
- **비금속·할로겐**: 전자를 **탈취** → 흡혈, 디버프, 단일 고화력
- **귀족 기체**: 옥텟 완성 → **반응하지 않음.** 최강 방어, 최약 공격
- 전자를 다 잃으면 행동 불능, 과잉 흡수하면 불안정 상태(자해 피해)

### 상성 — 실제 폴링 전기음성도로 계산

```
데미지 배율 = 1 + (공격자 전기음성도 − 방어자 전기음성도) × 0.35
```

- 값은 `src/data/elements.js`의 `electroneg` 필드에 실측치로 들어 있다
- 예: F(3.98)가 Na(0.93)를 → `1 + (3.98 − 0.93) × 0.35 = ×2.07`
- **귀족 기체는 `electroneg: null`** → 예외 규칙으로 주고받는 모든 피해 **×0.4**

### 전투 유형 3분류

| 유형 | 해당 족 | 근거 | 특성 |
|---|---|---|---|
| **무기형** | 전이 금속, 귀금속, 알칼리(토)금속 | 금속은 실체가 있어 무기가 된다 | 근접, 높은 HP·방어, **상성 배율 절반만 받음** |
| **마법형** | 비금속, 할로겐, 귀족 기체, 방사성 | 기체·비금속은 형체가 없다 | 원거리, 낮은 HP, **상성 배율 온전히 받음** |
| **하이브리드** | 준금속 (Si·As·Sb·B) | 금속과 비금속의 경계 | 근접·원거리 전환, 중간 성능 |

설정이 규칙을 이기는 예외: 마그네슘(세계수)→마법형, 폴로늄(엔지니어)→하이브리드, 염소(총기)→하이브리드.
`elements.js`의 `combat` 필드에 이미 반영되어 있다.

### 화합물 조합 (11종)

`R` 키로 쓴다. 전자를 소모하고 6초 쿨다운.
**효과가 전부 그 물질의 실제 성질에서 나온다** — 그래서 조합표를 외우지 않아도
무슨 일이 일어날지 화학으로 짐작할 수 있다.

| 조합 | 결과 | 효과 | 왜 그 효과인가 |
|---|---|---|---|
| Na + Cl | 소금 NaCl | 직선 관통 | 이온 결정이 단단하다 |
| H + O | 물 H₂O | 밀어냄, **알칼리 금속에 연쇄 폭발** | 실제로 격렬히 반응한다 |
| H + O | **얼음 H₂O** | 15초간 이동 2.1배 | 마찰이 거의 없다. 물과 같은 분자의 다른 상태 |
| Fe + O | 녹 Fe₂O₃ | 방어 −40% | 금속을 삭힌다 |
| C + O | 이산화탄소 CO₂ | 광역 질식 | 공기보다 무거워 낮게 깔린다 |
| Si + O | 수정 SiO₂ | 피해 흡수 장벽 | 그물 구조라 단단하다 |
| Ca + F | 형석 CaF₂ | 주변의 적을 드러냄 | 자외선에 빛난다 |
| Zn + S | 섬아연석 ZnS | 체력 25% 회복 | 빛을 저장했다 내놓는다 |
| Hg + S | 진사 HgS | 적 하나를 잠시 아군으로 | 사람을 홀리는 안료 |
| Pb + S | 방연석 PbS | 받는 피해 절반 | 납은 밀도가 높아 방사선을 막는다 |
| Ag + Br | 브로민화은 AgBr | 직전 화합물을 한 번 더 | 빛을 기억한다 (필름의 원리) |

물과 얼음이 같은 재료인 것이 핵심이다 — **같은 화합물의 다른 상태**이고,
도감이 그 상변화를 설명한다.

### 인연 파티 시스템

`elements.js`의 `bonds` 배열이 자료의 "인연이 깊은 원소"다. `areBonded(a, b)`로 판정한다.

- 철 + 망가니즈 → 합금 무기 제작 해금
- 금 + 은 + 구리 → 11족 형제 재회, 전용 합체기
- 나이오븀 + 탄탈럼 → 부모-자식, 상호 부활
- 코발트 + 니켈 → 강철부대 진형, 방어력 대폭 상승

---

## 7. 세계관 (구현 시 참조)

### 세력

| 세력 | 리더 | 구성원 | 성격 |
|---|---|---|---|
| 전이 금속 군단 | 철(Fe) | Co, Ni, Mn, Ti, Sc | 귀금속·비금속의 평등 사회를 내세우는 군대 |
| 귀금속 귀족 | 백금(Pt) | Au, Ag, Cu | 아름다움과 질서. 11족 3형제는 수천 년째 흩어져 있음 |
| 귀족 기체 | 네온(Ne) | Ar, Rn, He | 반응하지 않음. 최고재판소·아르곤 시티·온천 운영 |
| **전자 친화팀** | **염소(Cl)** | Br, F | **주적.** 아르곤의 전자 배치를 얻으려 무차별 전자 탈취 |
| 추방된 방사성 | 우라늄(U) | Po, Tc, Es, Rn | 대륙에서 쫓겨남. 적대적이지 않으나 불안정 |
| 중립 학자·주민 | — | C, Si, N, O, Ca, P, S, Mg, Zn, Bi, Hg, Pb, As, Sb, Be, Nb, Ta | 퀘스트 제공자 |

### 땅 — 손으로 지은 것과 절차적으로 나는 것

대륙은 두 층으로 되어 있다.

**손으로 지은 세계 (원점 ~220m).** 토룡마을과 그 밖의 고원·강·폐허·평야·바깥숲.
건물·길·다리가 y=0에 놓여 있으므로 이 범위의 지형은 **완전한 평지로 고정**된다.

**절차적 대륙 (225m 밖, 사실상 무한).** 시드에서 계산되는 바이옴 10종.
아래 표의 "이야기상 지역"은 아직 손으로 짓지 않았고, 그 자리를 대신할
바이옴이 이미 돌아가고 있다.

| 바이옴 | 화학적 근거 | 대응하는 이야기상 지역 |
|---|---|---|
| 초원 | — | 토룡마을 주변 |
| 마그네슘 숲 | 엽록소 한가운데에 마그네슘이 있다 | 숲의 주인(Mg) |
| 석회암 고원 | CaCO₃가 빗물에 녹아 동굴이 뚫린다 | 석회암 동굴(Ca) |
| 소금 평원 | NaCl 결정이 입방정계라 정육면체로 솟는다 | — |
| 붉은 대지 | 철이 산소에 전자를 빼앗긴 Fe₂O₃ | 철의 요새(Fe) |
| 수정 지대 | SiO₂가 육각기둥으로 자란다 | 규소의 관측소(Si) |
| 유황 온천 | 황이 115도에 녹아 땅속을 흐른다 | 호르메시스 온천(Rn, S) |
| 방사성 황무지 | 붕괴 에너지가 빛으로 나온다 | 추방지(U, Po) |
| 아르곤 고원 | 옥텟이 차 있어 반응하지 않는다 | 아르곤 시티(Ar, Ne) |
| 불안정한 바다 | — | **안정의 섬**(Og, Nh, 최종) |

**아직 손으로 지어야 할 곳**: 탄소 학교(C, Si) · 아르곤 시티 & 최고재판소 ·
철의 요새 · 거울 나라/황금 신전(Ag, Au) · 스티비 대성당(Sb) · 저승(Nb, Ta)

### 메인 스토리 7장

1. **이름 없는 자** — 토룡마을에서 깨어남. 칼슘이 전자 다루는 법을 가르친다
2. **전자 도둑** — 주민들의 전자가 사라진다. 범인은 전자 친화팀. 염소와 첫 대면
3. **갈라진 대륙** — 철의 평등 사상 vs 백금의 귀족주의. **플레이어가 편을 고른다**
4. **흩어진 형제** — 금·은·구리를 재회시키면 11족의 힘을 얻는다
5. **폭주** — 염소가 플루오린을 끌어들여 대륙의 전자를 빨아들이려 한다
6. **추방된 자들** — 방사성 지대에서 우라늄에게 안정의 섬의 존재를 듣는다
7. **안정의 섬** — 오가네손의 배로 불안정한 바다를 건넌다. 이름을 얻는다

### 보스 6인

| # | 보스 | 등급 | 장 | 패턴 |
|---|---|---|---|---|
| 1 | 브로민(Br) | 조무래기 | 2장 | 가짜 뉴스로 **잘못된 상성 배율**을 화면에 표시 |
| 2 | 비소(As) | 조무래기 | 2장 | 독을 깔지만 허술해서 **자기 독에 걸린다** |
| 3 | 수은(Hg) | 중급 | 3장 | **매혹**으로 아군을 조종. 액체라 형태 변화 회피 |
| 4 | 철(Fe) / 백금(Pt) | 중급 | 3장 | **세력 선택의 결과.** 중립이면 **둘 다** 상대 |
| 5 | 염소(Cl) | 중급 | 5장 | 전자 흡수로 아르곤 흉내. **처치/설득 두 갈래** |
| 6 | **폴로늄(Po)** | **파멸급** | 최종 | 3페이즈. 발명가 → 시작(試作) → **임계(붕괴 타이머)** |

### 엔딩 4종

| 엔딩 | 조건 | 내용 |
|---|---|---|
| 평등의 대륙 | 철 지지 + 전이 금속 평판 최대 | 귀금속 특권 해체. 플레이어는 `강철의 형제`가 된다 |
| 불변의 질서 | 백금 지지 + 귀금속 평판 최대 | 귀족 체제 유지. 플레이어는 `제119의 귀족`이 된다 |
| **안정의 섬** (진) | 중립 + 도감 90%↑ + 오가네손 동행 + **염소 설득** | 119번 자리가 주기율표에 새겨진다. 방사성 원소도 돌아온다 |
| 붕괴 (배드) | 염소에게 전자를 넘김 / 폴로늄 타이머 초과 | 모든 원소가 형태를 잃고 균질한 플라스마가 된다 |

---

## 8. 실행과 검증

### 로컬 실행

```
npx serve -l 3000
```

브라우저에서 `http://localhost:3000` 을 연다.
`file://` 로 열면 ES 모듈이 CORS로 막히므로 **반드시 서버로 띄울 것.**
포트가 점유되어 있으면 다른 포트를 쓰거나 기존 프로세스를 정리한다.

### 모바일 확인

Vercel 주소를 휴대폰에서 연다. 배포 전이면 같은 와이파이에서 PC의 로컬 IP로 접속한다.

### 단계별 검증

| 단계 | 확인할 것 |
|---|---|
| 1 | PC·모바일 양쪽 이동/점프/시선/`V` 시점 전환, 벽 통과 불가, **프레임률을 낮춰도 이동 속도 동일** |
| 3 | NPC 우클릭 → 성격 살아있는 대사 → 도감 등록 |
| 4 | 같은 적을 마법형·무기형으로 때렸을 때 **마법형만 상성 배율 온전히 받음**. 접근 시 카메라 락온 |
| 5 | 탄소 퀘스트 완료 → 탄소가 파티 후보에 추가. 철+망가니즈 편성 시 합금 제작 해금 |
| 6 | **브라우저 완전 종료 후 재접속 → 진행 복원** |
| 8 | 보스 처치 시 그 원소 획득. 저장 4개로 엔딩 4종 재생 |
| 9 | PC(호스트) + 휴대폰(게스트) 2대에서 위치·전투 결과 일치 |

### 커밋 전 검사

```
bash tools/check.sh
```

문법·import·화학·지형을 한 번에 본다. 자세한 내용은 「4. 마지막 작업 지점」 참조.

문법 검사가 `.mjs`로 복사해서 도는 이유는, ES 모듈이라 확장자를 바꾸지 않으면
`node --check`가 CommonJS로 해석해 실패하기 때문이다.

---

## 9. 캐릭터 3D 에셋

절차적 생성이 기본이므로 **에셋이 없어도 게임은 완전히 돌아간다.**
사용자가 만들면 `assets/models/<원소id>.glb`에 넣고, `CharacterLoader`가 자동으로 대체한다.

- 안내서: `concept/3d-character-guide.html` (이미지 생성 프롬프트 8종 포함)
- 스타일 비교: `concept/character-style-preview.html`
- **Meshy·Tripo는 다운로드/생성 단계에서 과금** — 실제로 확인됨
- 무료 대안: **VRoid Studio**(무료·무제한·리깅 포함·게임용 폴리곤) 또는 Hugging Face Spaces
- 총 39명 (원소 38 + 플레이어). 우선순위: 플레이어·Ca·C·Si·P → 보스 6인 → 세력 리더 → 나머지

---

## 10. 알려진 이슈

### 지금 남아 있는 것

- **인연이 가리키는 원소 18종이 아직 없다.** 주석·텅스텐·알루미늄·몰리브데넘 등.
  그만큼 인연 보너스가 발동하지 못한다. 여럿이 동시에 가리키는 것부터 만들면 효율이 좋다
- **보스 기믹이 비어 있다.** 페이즈 전환과 자막은 되지만 포탑 소환·독 장판은
  뼈대만 있다. (매혹은 화합물 쪽에 구현했으니 재사용 가능)
- **VRM 9종 125MB.** 43명을 다 만들면 저장소가 감당하지 못한다. PNG→WebP로 절반
- **손으로 지은 지역이 토룡마을 하나뿐.** 나머지 5곳은 절차적 지형만 있다
- `animateCharacter`가 `hover` 캐릭터의 `position.y`를 직접 쓰는데 `syncMesh`도 y를 쓴다.
  **부유형(귀족 기체) NPC를 `syncMesh`로 다루면 y가 중복 적용된다**

### 해결된 것 (같은 실수를 반복하지 않기 위해 남긴다)

- ~~Vercel 연결 미확인~~ → 연결됨. `git push`로 자동 배포.
  **공유 설정은 사용자만 건드린다**
- ~~포트 3000 점유~~ → 3100을 쓴다. `serve.json`이 `no-store`를 보내 캐시도 막는다

### 이 프로젝트에서 반복된 실패 유형

같은 종류의 실수가 세 번 이상 나왔다. 새 기능을 붙일 때 먼저 확인할 것.

1. **하드코딩된 좌표가 나중에 배신한다.**
   `y = 0`, `y = 1.15`, 하늘돔 반경 400m, 마을 평지 62m — 전부 "지형이 평지이고
   세계가 178m 안에 있다"는 전제 위에 있었다. 전제가 깨지자 한꺼번에 터졌다.
   → 절대값을 쓰기 전에 "이 값이 언제 틀려지는가"를 묻는다

2. **데이터만 적고 아무도 읽지 않는다.**
   화합물 효과 4종, `debuff`, `charmed` — 값은 예쁘게 들어가 있는데 소비하는
   쪽이 없어 아무 일도 일어나지 않았다.
   → 데이터를 추가하면 **읽는 쪽을 같은 커밋에** 넣는다

3. **검사 범위가 실제 콘텐츠보다 좁으면 검사가 의미 없다.**
   지형 검사는 55m까지만 평지를 확인했는데 마을은 220m까지 뻗어 있었다.
   화합물 검사는 원소 존재 여부를 안 봐서 제작 불가 화합물 3종을 놓쳤다.
   → 검사를 짤 때 "실제로 얼마나 넓은가"를 먼저 잰다

4. **도구 자체의 버그를 결과로 착각한다.**
   import 검사기의 정규식 이스케이프가 깨져 36건의 가짜 오류를 냈고,
   지면 탐침이 하늘돔을 지형으로 세어 400m 오차를 보고했다.
   → 검사가 이상한 결과를 내면 코드보다 검사기를 먼저 의심한다

# Elemental Expedition

원소를 의인화한 3D RPG. Three.js + 순수 ES 모듈, 빌드 도구 없음.

## 세션을 시작하면 가장 먼저 할 일

**`PLAN.md`를 끝까지 읽는다.** 거기에 전체 설계와 진행 상황이 들어 있다.

1. `PLAN.md`의 「4. 마지막 작업 지점」을 먼저 본다 — 중단 지점과 **바로 해야 할 일**이 적혀 있다
2. 「3. 진행 상황」에서 **첫 미완료 체크박스**를 찾는다 — 그것이 다음 작업이다
3. 작업을 마치면 **체크박스와 「4. 마지막 작업 지점」을 갱신하고 커밋한다**

계획을 다시 세우거나 사용자에게 "무엇부터 할까요"라고 묻지 말 것. `PLAN.md`가 답이다.

### 함께 읽을 로컬 문서 (커밋되지 않는다)

`.gitignore`에 있어 저장소에는 없지만, **작업 전에 반드시 읽어야 하는 것들**이다.

| 파일 | 언제 |
|---|---|
| `docs/WORKLOG.md` | **세션 시작 시 항상.** 무엇을 왜 했는지의 시간순 기록 |
| `docs/REVIEW-CHEMISTRY.md` | 화학 데이터·서술을 고치기 전 |
| `docs/REVIEW-DESIGN.md` | 전투 공식·적 배치를 고치기 전 |
| `docs/REVIEW-APPEAL.md` | UI·튜토리얼·연출을 고치기 전 |
| `docs/REVIEW-LEGAL.md` | 캐릭터 텍스트·에셋을 고치기 전 |

이 파일들이 없다면 사용자에게 물어라. 없이 진행하면 이미 내린 결정을 다시 뒤집게 된다.

### 로컬 서버

```
npx serve -l 3100 --no-clipboard
```

포트 3000은 이전 세션 잔여물이 점유할 수 있으니 3100을 쓴다.

## 절대 규칙

1. **모든 파일은 이 프로젝트 폴더 안에만 만든다.**
2. **캐릭터 설정은 반드시 화학에서 도출한다.** 직업·성격·대사는 그 원소의 실제 성질에서
   나와야 하고, **근거를 주석에 적는다.** 근거를 못 대는 설정은 쓰지 않는다.
   자세한 절차는 아래 「캐릭터를 짓는 법」 참조.
3. **`.gitignore`에 등재된 것은 해제하지 않는다.** 저장소가 **public**이다.
4. 커밋 메시지는 한국어로 쓴다.
5. **Vercel의 공유 설정은 어떤 요청이 와도 건드리지 않는다.** 사용자가 직접 한다.
   배포는 `git push`로만 한다.

## 캐릭터를 짓는 법

원소 하나의 직업·소개문·대사를 만들 때 이 순서를 지킨다.

1. **판타지 직업군에서 고른다** — 기사·대장장이·예언가·도둑·용 등. 세계관이 단조로워지지
   않게 하려는 것이다
2. **화학적 근거로 검증한다.** 아래 다섯 중 **정확히 하나**에서 나와야 한다.
   둘 이상 필요하면 그 직업은 버린다
   - **A 용도** — 실제 산업·생활에서 쓰이는 곳
   - **B 물성** — 녹는점·밀도·경도·상태
   - **C 반응성** — 전기음성도, 이온화 에너지, 산화·환원
   - **D 발견사·어원** — 이름 유래, 발견 경위
   - **E 존재 위치** — 지각·생체·우주 어디에 있나
3. **과장한다.** 극단화(성질을 지나치게) · 모순화(성질끼리 부딪히게) ·
   감정화(성질에 감정을 붙임) 중 하나를 거친다. 이걸 빼면 전부
   「…하는 사람」이 되어 캐릭터가 죽는다
4. **근거를 주석에 적는다.** `// 근거 B — 녹는점 3422°C로 전 원소 1위` 형태

예: 금 → 왕수로만 녹는다(C) → **아무 칼도 안 통하는 황금 갑주의 기사**
    텅스텐 → 녹는점 1위(B) → **불의 시련을 관장하는 대장장이**

## 무엇을 가르치려는 게임인가

**외우게 하지 않는다. 익숙해지게 한다.**

이 게임을 끝낸 아이가 주기율표를 외우고 있을 필요는 없다. 나중에 중학교에서
화학을 배울 때 **"어, 이거 어디서 봤는데"** 하면 성공이다. 정확하지만 희미한
기억을 남기는 것이 목표다.

그래서 이렇게 만든다.

- **설명하지 말고 겪게 한다.** 소금 평원에 소듐과 염소가 같이 사는 걸 보는 편이
  이온 결합을 설명해 주는 것보다 오래 남는다
- **정확성은 절대 양보하지 않는다.** 희미해도 되지만 틀리면 안 된다.
  나중에 배울 때 되살아나야 하는데, 틀린 기억은 오히려 방해가 된다
- **다 채우게 만들지 않는다.** 도감을 100% 채워야 끝나는 구조로 만들지 않는다.
  수집을 강요하면 그때부터 공부가 된다
- **분량보다 인상.** 긴 설명 하나보다 짧고 그림이 그려지는 한 줄이 낫다


## 글은 초등학교 4~6학년이 읽는다

이 게임의 독자는 **초등 4~6학년**이다. 중학생도 보지만 기준은 초등학생에 맞춘다.
어려우면 안 읽고, 안 읽으면 아무것도 못 가르친다.

### 쓰지 않는 말

아래 단어가 글에 들어가면 그 문장은 다시 쓴다. 괄호로 병기하는 것도 하지 않는다.

> 오비탈 · 산화수 · 이온화 에너지 · 전자 배치 · 원자가 · 옥텟 · 쿨롱 ·
> 공유 결합 · 이온 결합 · 부동태 · 승화 · 동소체 · 촉매 · 환원 ·
> 자유 전자 · 껍질 · 배위 · 극성 · 표준 환원 전위

**전기음성도만 예외다.** 전투 배율의 근거라 화면에 계속 나오므로 피할 수 없다.
대신 처음 나올 때 **「전자를 잡아당기는 힘」**이라고 풀어 주고, 그 뒤로는
그냥 "당기는 힘"이라고 부른다.

### 대신 쓰는 말

| 어려운 말 | 이렇게 |
|---|---|
| 가장 바깥 껍질의 전자 | 남은 전자 / 마지막 하나 |
| 전자를 방출한다 | 전자를 준다 / 내던진다 |
| 안정한 전자 배치가 된다 | 마음이 편해진다 / 자리가 딱 맞는다 |
| 반응성이 크다 | 아무하고나 잘 붙는다 / 가만히 못 있는다 |
| 산화된다 | 녹슨다 / 산소한테 뺏긴다 |
| 이온 결합을 한다 | 하나가 주고 하나가 받아서 붙는다 |
| 녹는점이 높다 | 어지간한 불로는 안 녹는다 |

### 문장 규칙

1. **한 문장에 개념 하나.** 쉼표로 두 가지를 잇지 않는다
2. **한 문장 40자 안쪽.** 길면 자른다
3. **아이가 아는 것에 먼저 빗댄다.** 원리는 그다음이다
4. **소개문은 3문장**, 대사는 1~2문장 반말
5. **숫자는 혼자 두지 않는다.** 옆에 비유를 붙인다
   - ✗ `녹는점 3422°C`
   - ✓ `녹는점 3422°C — 초를 켜서는 어림도 없다`

### 이렇게 바뀐다

> ✗ 가장 바깥 껍질에 전자가 딱 하나뿐이다. 그 하나만 내주면 비활성 기체와
>   같은 안정한 배치가 되므로, 기회만 있으면 내던진다.
>
> ✓ 전자가 딱 하나 남았다. 그게 자꾸 거추장스럽다.
>   그래서 아무한테나 줘 버린다. 물만 만나도 불이 붙는 건 그래서다.

**검사**: `node tools/check-reading.mjs`가 금지어와 문장 길이를 잡는다.

## 코드 규약

- **빌드 없음.** `index.html`의 importmap이 three.js를 CDN에서 불러온다. npm 패키지를 추가하지 않는다
- **모든 시각 요소는 `src/fx/Style.js`를 거친다.** `toonMaterial()`·`makeOutline()`을 쓰고
  `MeshStandardMaterial`을 직접 만들지 않는다. 화면 전체의 톤이 갈리는 것을 막기 위해서다
- **논리 업데이트는 고정 틱**(`update(dt)`), 렌더는 `render(alpha)`로 보간한다.
  프레임률에 의존하는 코드를 쓰지 않는다
- **입력은 `src/core/Input.js`만 읽는다.** 게임 로직이 키보드/터치를 직접 알면 안 된다
- 주석은 한국어로, **무엇을 하는지가 아니라 왜 그렇게 했는지**를 적는다

## 실행

```
npx serve -l 3000
```

`http://localhost:3000` — `file://`로 열면 ES 모듈이 CORS로 막힌다.

## 커밋 전 검사

```
bash tools/check.sh
```

문법·import·화학·지형을 한 번에 본다. 넷 다 이 프로젝트가 겪은 실제 사고에서 나왔다.

- **문법** — `.mjs`로 복사한 뒤 `node --check`. ES 모듈이라 확장자를 안 바꾸면
  CommonJS로 해석해 실패한다
- **import 무결성** — 없는 이름을 import하면 `node --check`는 못 잡고 브라우저에서야 터진다
- **화학** — 틀린 값은 재미가 아니라 오개념을 가르친다
- **지형** — 청크 이음매·바이옴 분포·경사. 브라우저로는 먼 땅에 가 볼 수도 없다
- **읽기 난이도** — 금지어·문장 길이·비유 없는 숫자. 정확한데 안 읽히는 글은
  이 프로젝트에서 틀린 글과 같다. 아이가 안 읽으면 아무것도 못 가르친다

## 화면에 관한 일은 화면에서 재고 나서 말한다

계산이 맞아도 화면에서 틀릴 수 있다. 실제로 그랬다.

- `grid-column: var(--g)`는 CSS에서 무효다 — 커스텀 속성이 문자열로 취급돼
  격자 배치가 통째로 무시된다. 인라인 `style`에 값을 직접 쓴다
- 표본 몇 개로 "검증 통과"라고 말하지 않는다. 실제로 렌더해서 좌표를 잰다
- probe의 창 크기는 실제 사용자 화면에 맞춘다. 좁은 창으로만 재면
  "넘친다"는 판단이 계속 어긋난다

## 브라우저를 눈으로 볼 수 없을 때

버그의 절반이 "화면이 이상하다"로 들어오는데 화면을 볼 수가 없다.
헤드리스 크롬으로 **숫자를 재는** 도구가 있다.

```
node tools/probe.mjs [주소] [대기ms] ["평가할 JS"]   # 콘솔 오류 + 화면 상태
node tools/probe-walk.mjs [주소] [초]                # 베타로 시작해 걸어 본다
node tools/probe-ground.mjs [주소]                   # 지면과 소품 높이를 잰다
node tools/probe-npc.mjs [주소]                      # NPC에 실제로 닿을 수 있는가
node tools/probe-codex.mjs [주소]                    # 도감을 다섯 화면 크기에서 잰다
node tools/probe-npc.mjs [주소]                      # NPC에게 다가갈 수 있는지 잰다
```

`window.__debug = { scene, world, player, camera, renderer }` 가 열려 있다.
**게임 로직은 이걸 절대 읽지 않는다** — 읽는 순간 디버그 손잡이가 사양이 된다.

## 더 읽을 것

- `docs/WORLD.md` — 무한 지형을 고치기 전에 반드시. 높이를 아는 곳은 한 군데뿐이다
- `docs/CHEMISTRY.md` — 화학 데이터를 고치기 전에. 규칙이 곧 화학이다

## 배포

`git push` → Vercel 자동 배포 (`kturtleman100-glitch/Elemental-Expedition`).

# 개인 자료 보관함 — 저장소에 올리지 않는다.
참고자료/
참고자료/**

# 개발 중 시안 문서. 게임 실행과 무관하며 배포물에 포함하지 않는다.
concept/

# 내부 검토 문서 · 작업 로그. 로컬에서만 본다.
docs/REVIEW-*.md
docs/VERIFY-*.md
docs/WORKLOG.md

# 에디터
.vscode/
.idea/
*.swp

# OS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# 의존성 / 빌드 (현재는 쓰지 않지만 대비)
node_modules/
dist/
.vercel/

# 로그
*.log
npm-debug.log*

# VRoid 원본 작업 파일 — assets/models/ 에 복사본이 들어가므로 중복 커밋하지 않는다
캐릭터 3d 모델/

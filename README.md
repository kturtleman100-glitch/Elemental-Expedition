# Elemental Expedition

원소를 의인화한 3D RPG. 캐릭터의 직업과 성격이 그 원소의 실제 화학적 성질에서 나옵니다. Three.js(WebGL) 기반, 빌드 도구 없이 CDN + importmap으로 동작합니다.

## 실행 방법

`index.html`을 브라우저로 직접 열면 `file://` 프로토콜에서 ES 모듈이 CORS로 막힐 수 있습니다. 로컬 서버로 띄우세요.

```powershell
npx serve .
```

그 다음 안내되는 주소(보통 `http://localhost:3000`)로 접속합니다.

### 같은 와이파이의 휴대폰에서 확인

```powershell
npx serve . --host 0.0.0.0
```

PC의 로컬 IP(예: `192.168.0.12`)를 확인한 뒤 휴대폰 브라우저에서 `http://192.168.0.12:3000` 으로 접속합니다.

### 배포된 주소로 확인

`main` 브랜치에 push하면 Vercel이 자동으로 배포합니다. 배포 주소는 `https://<프로젝트>.vercel.app` 형태입니다.

## 조작

| 동작 | PC | 모바일 |
|---|---|---|
| 이동 | WASD | 좌측 가상 조이스틱 |
| 시선 | 마우스 (클릭 시 포인터 락) | 우측 화면 드래그 |
| 공격 | 좌클릭 | 공격 버튼 |
| 상호작용 | Enter · F · 우클릭 | 상호작용 버튼 |
| 점프 | Space | 점프 버튼 |
| 인벤토리 | E | 상단 메뉴 |
| 빠른 이동 | M (쉼터에서) | — |
| 도감 | K | 상단 메뉴 |
| 파티 편성 | P | 상단 메뉴 |
| 시점 전환(1인칭/3인칭) | V | 시점 버튼 |

## 개발 상태

`PLAN.md` 기준 8단계까지 구현했고, 지금은 상업화를 위한 재설계를 진행 중입니다.

캐릭터의 직업과 성격은 전부 그 원소의 실제 화학적 성질에서 도출합니다 —
금은 산소와 반응하지 않아 수천 년을 견디므로 녹슬지 않는 갑주의 기사이고,
아연은 철 대신 자기가 먼저 삭으므로 남을 대신해 녹슬어 주는 도장공입니다.
근거는 코드 주석에 남깁니다. 설정이 곧 학습 내용이 되게 하려는 것입니다.

대상 독자는 초등 4~6학년입니다. 주기율표를 외우게 하는 것이 아니라,
나중에 중학교에서 배울 때 "어디서 봤는데" 하고 떠오르도록
정확하지만 희미한 기억을 남기는 것이 목표입니다.

## 문서

| 문서 | 언제 읽나 |
|---|---|
| `PLAN.md` | 세션을 시작할 때. 진행 상황과 다음에 할 일 |
| `docs/WORLD.md` | 지형·청크·바이옴을 고치기 전에 |
| `docs/CHEMISTRY.md` | 원소·화합물 데이터를 고치기 전에 |
| `CLAUDE.md` | 프로젝트 규약 |

## 캐시 정책

`vercel.json`이 모든 파일에 `no-cache`를 건다. 브라우저는 여전히 캐시하지만
매번 서버에 물어보고(대개 304로 본문 없이 끝난다) 최신인지 확인한다.

예전에는 `/src/`에 `max-age=3600`을 걸어 뒀는데, 빌드 도구가 없어 파일 이름에
해시가 붙지 않으므로 배포 직후 한 시간 동안 **옛 모듈과 새 모듈이 섞여** 들어왔다.
모듈 하나만 옛것이어도 import가 어긋나 로딩이 0%에서 멈춘다.

개발 서버는 `serve.json`이 `no-store`를 보내 아예 캐시하지 않는다.
